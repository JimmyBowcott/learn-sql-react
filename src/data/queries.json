{
  "clauses": {
    "selection": ["SELECT", "FROM"],
    "filtering": ["WHERE", "IN", "NOT IN", "BETWEEN", "LIKE", "ILIKE", "IS NULL", "IS NOT NULL"],
    "ordering": ["ORDER BY"],
    "limiting": ["LIMIT", "OFFSET", "FETCH FIRST"],
    "aliasing": ["AS"],
    "aggregation": ["COUNT", "SUM", "AVG", "MIN", "MAX"],
    "grouping": ["GROUP BY", "HAVING"],
    "joins": ["JOIN", "INNER JOIN", "LEFT JOIN", "RIGHT JOIN", "FULL JOIN", "ON", "USING", "NATURAL JOIN"],
    "subqueries": ["EXISTS", "NOT EXISTS", "ANY", "ALL"],
    "set_operations": ["UNION", "UNION ALL", "INTERSECT", "EXCEPT"],
    "insertion": ["INSERT INTO", "VALUES", "DEFAULT"],
    "updates": ["UPDATE", "SET"],
    "deletion": ["DELETE FROM", "TRUNCATE"],
    "transactions": ["BEGIN", "COMMIT", "ROLLBACK"],
    "schema_creation": ["CREATE TABLE", "CREATE INDEX", "CREATE VIEW", "CREATE DATABASE"],
    "schema_modification": ["ALTER TABLE", "ADD", "DROP", "RENAME"],
    "schema_removal": ["DROP TABLE", "DROP INDEX", "DROP VIEW"],
    "constraints": ["PRIMARY KEY", "FOREIGN KEY", "UNIQUE", "NOT NULL", "CHECK", "DEFAULT"],
    "window_functions": ["OVER", "PARTITION BY", "RANK", "DENSE_RANK", "ROW_NUMBER"],
    "cte": ["WITH", "RECURSIVE"],
    "conditional": ["CASE", "WHEN", "THEN", "ELSE", "END"],
    "functions": ["CAST", "COALESCE", "NULLIF", "GREATEST", "LEAST"]
  },
  "modifiers": {
    "filtering_modifiers": ["DISTINCT", "TOP", "ONLY"],
    "ordering_modifiers": ["ASC", "DESC"]
  },
  "operators": {
    "comparison": ["=", "!=", "<>", ">", "<", ">=", "<="],
    "logical": ["AND", "OR", "NOT"],
    "arithmetic": ["+", "-", "*", "/", "%"]
  }
}
